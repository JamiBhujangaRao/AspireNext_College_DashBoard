
import { LogoutContainer, LogoutMessage } from './styledComponents';

const Logout = () => (
  <>
    <LogoutContainer>
      <LogoutMessage>You have been logged out</LogoutMessage>
    </LogoutContainer>
  </>
);

export default Logout;
